export interface VerbConjugation {
  ich: string;
  du: string;
  erEsSie: string;
  wir: string;
  ihr: string;
  SieSie: string;
  verbName?: string; // Optional: to display the verb name if needed later
  partizipII?: string; // Optional: to include the past participle form
}